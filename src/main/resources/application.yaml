ktor:
  deployment:
    port: 8080
  application:
    modules:
      - com.example.ApplicationKt.configure

database:
  jdbcUrl: "jdbc:postgresql://localhost:5432/posterDb"
  driverClassName: "org.postgresql.Driver"
  username: "postgres"
  password: "postgres"
  maximumPoolSize: 10
  minimumIdle: 2
